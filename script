local Material = loadstring(game:HttpGet("https://raw.githubusercontent.com/Kinlei/MaterialLua/master/Module.lua"))()
local UI = Material.Load({
     Title = "A Menacing Timeline Gui",
     Style = 3,
     SizeX = 400,
     SizeY = 300,
     Theme = "Dark"
     })

local Page = UI.New({
    Title = "Astrix was here"
})
Page.Button({
    Text = "Click for creds (SENDS IN CHAT)",
    Callback = function()
       local ohString1 = "Creds to Astrix for fixing it! otherwise Ahi did the scripts =) and join on cord .gg/wYukG5G3"
local ohString2 = "All"
game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer(ohString1, ohString2)
    end
})
local Page2 = UI.New({
    Title = "Scripts"
})
Page2.Toggle({
    Text = "KQ attacks",
    Callback = function(value)
    _value = value
    while _value do task.wait(0.3)

         game:GetService("ReplicatedStorage").Coin:FireServer()
    local ohNumber1 = 60
game:GetService("ReplicatedStorage").BombDamage:FireServer(ohNumber1)

    end
    Enabled = true
    end

   })
   
Page2.Toggle({
    Text = "The world knife spam (might be laggy)",
    Callback = function(value)
    _value = value
  
getgenv().loop = true

while _value do task.wait() 



game:GetService("ReplicatedStorage").HFTFKnife:FireServer()
    
task.wait(0)
end
    Enabled = true
    end
   })



Page2.Toggle({
    Text = "hierophanto splash",
    Callback = function(value)
    _value = value
    while _value do wait(0.8)

  

game:GetService("ReplicatedStorage").TwentyMeterSplash:FireServer()

    end
    Enabled = true
    end
    
   })
   
       local Page3 = UI.New({
        Title = "Player"
    })

       Page3.Button({
        Text = "Press this to heal yourself",
        Callback = function()
    
    
            local ohString1 = "KillerQueenStand"
            local ohNumber2 = 50
            local ohNumber3 = 455
            
            game:GetService("ReplicatedStorage").Stand:FireServer(ohString1, ohNumber2, ohNumber3)
    
    
    
    
           print("Clicked!") 
        end
    })

Page2.Toggle({
    Text = "vampire attack",
    Callback = function(value)
    _value = value
    while _value do task.wait(0.2)

game:GetService("ReplicatedStorage").SpaceRipperStingyEyes:FireServer()

    end
    Enabled = true
    end

   })


Page3.Toggle({
    Text = "walk faster (Reset to stop)",
    Callback = function(value)
    _value = value
    while _value do task.wait(0.2)

local plr = game.Players.LocalPlayer
	local char = plr.Character

	char.Humanoid.WalkSpeed =50

    end
    Enabled = true
    end

   })
   
   local Page4 = UI.New({
    Title = "funi stuuff"
})

Page4.Toggle({
    Text = "Rave",
    Callback = function(value)
    _value = value
    while _value do task.wait(0.6)

-- This script was generated by Hydroxide's RemoteSpy: https://github.com/Upbolt/Hydroxide

local ohBrickColor1 = BrickColor.new("Institutional white")

game:GetService("ReplicatedStorage").Jump:FireServer(ohBrickColor1)

    end
    Enabled = true
    end

   })

